BUILD
-----
The nuttx.bin binary file has been compiled with the following command from nuttx folder:

$ ./tools/configure.sh stm32h743vit6-ct2022:nsh
$ make

with default nsh config


EXPECTED BEHAVIOR:
-----------------
In serial console, we should see :

NuttShell (NSH)
nsh>

